# DeepSeek Harness 插件开发指南（工作参考）

> 本文件是给后续插件开发用的速查指南，内容均来自仓库内 `docs/` 与 `packages/` 的真实约定。
> 仓库根目录：`/Users/mac/Documents/脚本/deepseek-harness`

## 0. 项目定性

DeepSeek Harness 是基于 vendored Cordis 的「微内核」agent harness——**万物皆插件**，没有需要 patch 的特权核心。扩展能力 = 在别的插件旁边挂一个自己的插件；所有注册都走 `ctx.effect()`，插件卸载时自动回滚。完整说明见 `docs/architecture.md`。

## 1. 用什么语言

- **TypeScript，全程 ESM**（`"type": "module"`），禁止 CJS-only 导出。
- 每个包名必须是 `@deepseek-ai/dsh-<name>`。
- 源码内相对引用带 `.ts` 后缀（`export * from './types.ts'`），编译器改写为 `.js`。
- 有两个包形态：
  - **函数插件（function plugin）**：具名导出 `name` / `inject` / `Config` / `apply`，**不要 default export**（混用会被 Loader 丢弃命名空间）。
  - **服务包（service package）**：`default` 导出 Service 子类。
- （另有一个 Python SDK 在 `python/`，但写 harness 插件主语言是 TS。）

## 2. 写在哪里

### A. 仓库内（in-tree，推荐做正式能力）
放到 `packages/<group>/<pkg>/`，复用已有 group：`core / llm / shell / fs / skill / web / hooks / session / bundle / terminal / subagent / workflow / todo / plan ...`
完整分组清单：`/Users/mac/Documents/脚本/deepseek-harness/packages/README.md`

目录结构（逐文件清单见 `docs/cookbook/adding-a-package.md`）：
```
/Users/mac/Documents/脚本/deepseek-harness/packages/<group>/<pkg>/
  package.json     # 从 packages/core/tools 复制改 name/deps
  tsconfig.json    # extends ../../../tsconfig.base.json
  src/index.ts     # 函数插件的 name/inject/apply
  README.md        # 必含 Model Experience + Known Limitations 段
```
新包还要在 `tsconfig.host.json` 或 `tsconfig.client.json` 的 `references` 登记（只能属于一个聚合）。

### B. 仓库外（out-of-tree）
profile 可持有「树外插件」和自己的 `cordis.patch.yml`，用命令装进某个 profile：
```sh
dsh plugin --profile <name> add <package>
```
Bundle 也是这种形态：`package.json` 声明 `"dsh": { "bundle": { "patch": "./cordis.patch.yml" } }`，即可安装的 patch 层。

## 3. 开发环境

- **Node.js 22.19+ 或 24+**（CI 覆盖 22.19 / 24 / 26）。
- **pnpm（Corepack 管理，仓库锁定 pnpm@11.7.0）**：`corepack enable` 后用 `pnpm install`。
- Git 2.26+（装 Lefthook 钩子）。
- 可选 `DEEPSEEK_API_KEY`（放根目录 `.env`），跑真实 API 的 demo/e2e 才需要。

首次搭建：
```sh
cd /Users/mac/Documents/脚本/deepseek-harness
pnpm install
pnpm run typecheck   # 通过即环境就绪
```

## 4. 插件长什么样（最小真实示例）

拦截工具执行的 hook 插件（`docs/cookbook/extension-cookbook.md` 原文）：
```ts
import type { Context } from '@deepseek-ai/cordis'
import type { PreToolDecision, ToolExecution } from '@deepseek-ai/dsh-tools'

export const name = 'permission-gate'
export function apply(ctx: Context) {
  ctx.on('tools/pre-execute', async (exec, next): Promise<PreToolDecision> => {
    if (!(await isAllowed(exec))) return { kind: 'deny', reason: 'Denied by policy.' }
    return next()
  })
}
```
扩展点都是 `ctx.on('agent/*')` / `ctx.on('tools/*')` / `ctx.on('session/event')` 这类事件（waterfall 要调 `next()`）。

### 能力 → 扩展点对照（高频）
| 想加的东西 | 机制 |
|---|---|
| 模型 provider | `ctx.llm.registerAdapter(...)` |
| 模型可见能力（工具） | `ctx.tools.register()`，schema 自动进 prompt 组装 |
| shell 执行 | 注册 `ctx.shell` backend |
| 文件系统/策略 | 注册 `ctx.fs` provider 或监听 `fs/*` |
| 人类命令 | `ctx.commands` |
| 后台任务 | `ctx.jobs` |
| 拦截请求/工具/turn | `agent/*` 或 `tools/*` 事件；`agent/turn-stopping` 可停 turn |
| 注入模型上下文 | `agent.inject()` |
| Web Chat 节点 | 注册 `ConversationNodeDefinition` + keyed renderer |
| 持久会话状态 | 扩展 `SessionEventMap`，从 log 渲染/replay |

完整表：`docs/architecture.md` 末尾「Where new behavior goes」+ `docs/cookbook/extension-cookbook.md` 的 feature→mechanism 表。

## 5. 部署 / 安装

### in-tree（随仓库构建）
```sh
pnpm install
pnpm run doc-sync
pnpm run constraints && pnpm run typecheck && pnpm run lint
pnpm run build && pnpm run hygiene
```
产物在 `lib/`（`lib/index.js` + `lib/types/*.d.ts`）。运行时靠 **profile 组合**挂上插件：
- 一个运行的 `dsh` = 按层叠加的插件树。层序：profile 列出的各 bundle → profile 的 `cordis.patch.yml` → home 级 patch → `--patch` 覆盖层。
- 看实际启动树：`dsh --profile web --dump-config`（打印的每行都能被自己的 patch 替换）。
- 跑：`pnpm dsh --profile headless "你的任务"`（需 `DEEPSEEK_API_KEY`）。

### out-of-tree / bundle
```sh
dsh plugin --profile <name> add <package>
```
profile 记住堆叠的 bundle、树外插件和 patch，下次启动自动加载。

### 改完即时生效
所有注册都是 `ctx.effect()` → Cordis HMR 直接生效，无需重启内核。

## 6. 关键路径速查（绝对路径）

- 仓库根：`/Users/mac/Documents/脚本/deepseek-harness`
- 写插件：`/Users/mac/Documents/脚本/deepseek-harness/packages/<group>/<pkg>/`
- 分组清单：`/Users/mac/Documents/脚本/deepseek-harness/packages/README.md`
- 包级规则：`/Users/mac/Documents/脚本/deepseek-harness/packages/AGENTS.md`
- Host 包登记：`/Users/mac/Documents/脚本/deepseek-harness/tsconfig.host.json`
- Client 包登记：`/Users/mac/Documents/脚本/deepseek-harness/tsconfig.client.json`
- 共享编译选项：`/Users/mac/Documents/脚本/deepseek-harness/tsconfig.base.json`
- 依赖/脚本：`/Users/mac/Documents/脚本/deepseek-harness/package.json`
- 架构/扩展点：`/Users/mac/Documents/脚本/deepseek-harness/docs/architecture.md`
- Cordis 入门：`/Users/mac/Documents/脚本/deepseek-harness/docs/cordis-primer.md`
- 第一个插件教程：`/Users/mac/Documents/脚本/deepseek-harness/docs/cordis-tutorial/01-first-plugin.md`
- 建包清单：`/Users/mac/Documents/脚本/deepseek-harness/docs/cookbook/adding-a-package.md`
- 扩展形态示例：`/Users/mac/Documents/脚本/deepseek-harness/docs/cookbook/extension-cookbook.md`
- 加工具：`/Users/mac/Documents/脚本/deepseek-harness/docs/cookbook/adding-a-tool.md`
- 加 LLM 适配器：`/Users/mac/Documents/脚本/deepseek-harness/docs/cookbook/adding-an-llm-adapter.md`
- 加 Web Chat 节点：`/Users/mac/Documents/脚本/deepseek-harness/docs/cookbook/adding-a-conversation-node.md`
- 开发环境/搭建：`/Users/mac/Documents/脚本/deepseek-harness/docs/development.md`
- vendored Cordis 源码：`/Users/mac/Documents/脚本/deepseek-harness/vendor/`

## 7. 不会 TS？用 Python 走 MCP（推荐）

Cordis 内核是 TypeScript，**原生插件（`apply(ctx)` 直接挂进插件树）必须用 TS 写**，Python 无法 mount 进插件树。
但有官方预留的「任意语言扩展」机制——**MCP（Model Context Protocol，语言无关）**：

- harness 自带桥接插件 `@deepseek-ai/dsh-mcp-client`（包路径：`/Users/mac/Documents/脚本/deepseek-harness/packages/mcp/mcp-client/`）。
- 用 Python 写一个 MCP server（stdio 或 streamable-http 传输），在 `cordis.yml` 加一个 entry，工具会自动注册成 `mcp__<serverName>__<rawName>` 原生工具给模型用，支持 HMR 热换与断线重连。

真实接线示例（`command` 可以是任意可执行，换成 `python` 即可）：
```yaml
- id: mcp-my-python
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    serverName: mypy
    transport: stdio
    command: python
    args: ['/abs/path/to/my_server.py']
    env:
      MY_TOKEN: !!js process.env.MY_TOKEN
```
完整配置字段：`/Users/mac/Documents/脚本/deepseek-harness/packages/mcp/mcp-client/README.md`

其他 Python 路径（都不是「挂进插件树」的内核插件）：
- **out-of-process runtime**：`python/` SDK 用 JSON-RPC over stdio 驱动已运行的 dsh runtime 子进程（外部集成，非插件）。路径：`/Users/mac/Documents/脚本/deepseek-harness/python/README.md`
- **ACP / JSON-RPC 协议驱动**：协议语言无关，`acp/` 包是 TS 实现，Python 也能按协议驱动 agent。

选型：只给模型加工具/能力 → 写 Python MCP server（零 TS）；要改内核行为（拦截请求、改 loop、加 ctx 服务）→ 必须 TS 插件。

## 8. 上手顺序
1. 只加工具/能力（不熟 TS）：先写 Python MCP server，配 `dsh-mcp-client` 桥（见第 7 节）。
2. 要写原生 TS 插件：`docs/cordis-primer.md` → `docs/cordis-tutorial/01-first-plugin.md` → `docs/architecture.md` → `docs/cookbook/adding-a-package.md`。
3. 按需看 `adding-a-tool` / `adding-an-llm-adapter` / `adding-a-conversation-node`。
